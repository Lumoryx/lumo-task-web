// Schema documentation — table definitions are in migrate.ts (raw SQL).
// Kept here as a reference for column names and types.
//
// Tables: users, tasks, completed_entries, people, settings
// All LocalizedString fields stored flat: title_en / title_zh
// SQLite booleans stored as integer 0/1
export {};
